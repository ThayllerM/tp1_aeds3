Como compilar e executar o programa:

1_ Abra esse diretorio no terminal.
2_ Digite o comando "make".
3_ Escreva a entrada no arquivo "entrada.txt" (assim como diz na especificação do tp).
4_ Digite o comando "./tp -a <modo de execução>".

Modos de execução:

1_ FB (executa o algoritmo de força bruta).
2_ AG (executa o algoritmo guloso).
3_ PD (executa o algoritmo de programação dinamica).

versão do compilador:
gcc (Ubuntu 7.4.0-1ubuntu1~18.04.1) 7.4.0

